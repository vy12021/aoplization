open module aoplization.runtime {
  requires auto.common;
  requires aoplizatioin.annotation;
  requires java.base;
  requires java.compiler;
  requires aoplization.compiler;
  requires compile.testing;
  requires junit;
  requires truth;
}